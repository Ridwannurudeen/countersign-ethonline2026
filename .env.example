# Existing funded Hedera testnet operator. Use a canonical numeric account ID.
HEDERA_OPERATOR_ACCOUNT_ID=

# DER-encoded Hedera private key for the operator above. Never commit the real value.
HEDERA_OPERATOR_PRIVATE_KEY=

# Exact major.minor.patch values independently recorded from NetworkVersionInfoQuery.
# On the first credentialed run, the spike prints the current values and stops here if
# these are unset. Record them, set both variables, then rerun the complete proof.
# The values below are the versions Hedera testnet reported on 2026-09-12, which are the
# ones this guard's field-completeness review was audited against. Confirm them against
# what the spike prints rather than trusting them; the guard refuses after a schema change
# because the decoder cannot reveal fields unknown to the pinned protobuf schema.
COUNTERSIGN_ALLOWED_PROTOBUF_VERSION=0.76.3
COUNTERSIGN_ALLOWED_SERVICES_VERSION=0.76.3
